# Required: public URL of this site (used for sitemap, canonical, etc.)
BASE_URL= http://localhost:80

# Required: backend API URL (e.g. https://backend.dansday.com). In Docker use http://host.docker.internal:8080
BACKEND_URL= http://localhost:8080
