<button type="button" class="btn btn-outline-secondary btn-sm ai-generate-btn" data-type="{{ $type }}" data-field="{{ $field }}" data-input-name="{{ $inputName }}" data-summernote="{{ $summernote ? '1' : '0' }}" title="{{ __('content.generate_with_ai') }}">
    <i class="fas fa-magic me-1"></i>{{ __('content.generate_with_ai') }}
</button>
